# Alur Pendaftaran Pelanggan D'four Laundry

## 📊 Struktur Tabel Database

### Tabel `users` - Akun Login
| Kolom | Tipe | Keterangan |
|-------|------|------------|
| id | INT | Primary Key |
| google_id | VARCHAR | ID dari Google (jika login Google) |
| email | VARCHAR | Email user (unique) |
| name | VARCHAR | Nama lengkap |
| **phone** | VARCHAR | **Nomor HP (untuk linking ke customers)** |
| password_hash | VARCHAR | Password terenkripsi |
| profile_picture | TEXT | URL foto profil |
| role | ENUM | 'superadmin', 'admin', 'cashier', 'user' |
| login_method | VARCHAR | 'email' atau 'google' |
| is_active | BOOLEAN | Status aktif akun |
| email_verified_at | TIMESTAMP | Waktu verifikasi email |
| last_login | TIMESTAMP | Login terakhir |

### Tabel `customers` - Data Pelanggan Laundry
| Kolom | Tipe | Keterangan |
|-------|------|------------|
| id | INT | Primary Key |
| **user_id** | INT | **Foreign Key ke users (untuk linking)** |
| name | VARCHAR | Nama pelanggan |
| **phone** | VARCHAR | **Nomor HP (untuk matching dengan users)** |
| address | TEXT | Alamat |
| **registered_at** | TIMESTAMP | **Waktu user register/ter-link** |
| created_at | TIMESTAMP | Waktu dibuat |

### Tabel `transactions` - Transaksi Laundry
| Kolom | Tipe | Keterangan |
|-------|------|------------|
| id | INT | Primary Key |
| **customer_id** | INT | **Foreign Key ke customers** |
| service_type | VARCHAR | Jenis layanan |
| weight/quantity | DECIMAL | Berat/jumlah |
| price | DECIMAL | Harga total |
| status | ENUM | 'pending', 'washing', 'drying', 'ironing', 'done', 'picked_up' |
| created_at | TIMESTAMP | Waktu transaksi |

---

## 🔄 ALUR 1: Pendaftaran via Email/Password

### Langkah-langkah:

```
┌────────────────────────────────────────────────────────────────────┐
│  STEP 1: User buka /pages/auth/register.php                       │
│          Isi form: Nama, Email, No HP, Password                    │
└────────────────────────┬───────────────────────────────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────────────┐
│  STEP 2: JavaScript kirim data ke /api/auth/register.php          │
│          POST { name, email, phone, password }                     │
└────────────────────────┬───────────────────────────────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────────────┐
│  STEP 3: Validasi di Backend                                       │
│                                                                    │
│  ✓ Email belum terdaftar di users?                                 │
│  ✓ Phone belum terdaftar di users?                                 │
│  ✓ Format phone valid (08xxxxxxxxxx)?                              │
│  ✓ Password minimal 8 karakter?                                    │
└────────────────────────┬───────────────────────────────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────────────┐
│  STEP 4: INSERT ke tabel USERS                                     │
│                                                                    │
│  INSERT INTO users (email, name, phone, password_hash, role,       │
│                     login_method, is_active)                       │
│  VALUES ('ahmad@email.com', 'Ahmad', '081234567890',               │
│          '$2y$...hash...', 'user', 'email', 1)                     │
│                                                                    │
│  → User ID = 5                                                     │
└────────────────────────┬───────────────────────────────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────────────┐
│  STEP 5: CEK apakah Phone sudah ada di CUSTOMERS                   │
│                                                                    │
│  SELECT id FROM customers WHERE phone = '081234567890'             │
└───────────────┬────────────────────────────┬───────────────────────┘
                │                            │
        ┌───────┴───────┐            ┌───────┴───────┐
        │  TIDAK ADA    │            │  SUDAH ADA    │
        └───────┬───────┘            └───────┬───────┘
                │                            │
                ▼                            ▼
┌───────────────────────────┐    ┌───────────────────────────┐
│  STEP 6A: Buat CUSTOMER   │    │  STEP 6B: LINK ke         │
│  baru                     │    │  customer existing        │
│                           │    │                           │
│  INSERT INTO customers    │    │  UPDATE customers         │
│  (user_id, name, phone,   │    │  SET user_id = 5,         │
│   registered_at)          │    │      registered_at = NOW()│
│  VALUES (5, 'Ahmad',      │    │  WHERE id = 3             │
│   '081234567890', NOW())  │    │                           │
└───────────────────────────┘    └───────────────────────────┘
                │                            │
                └──────────────┬─────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────────────┐
│  STEP 7: Kirim email verifikasi (opsional, skip di localhost)      │
│          Redirect ke halaman login                                 │
└────────────────────────────────────────────────────────────────────┘
```

### Hasil di Database:

**Skenario A: Phone BARU (belum ada di customers)**
```
TABEL users:
┌────┬────────────────────┬────────┬──────────────┬────────┐
│ id │ email              │ name   │ phone        │ role   │
├────┼────────────────────┼────────┼──────────────┼────────┤
│ 5  │ ahmad@email.com    │ Ahmad  │ 081234567890 │ user   │
└────┴────────────────────┴────────┴──────────────┴────────┘

TABEL customers:
┌────┬─────────┬────────┬──────────────┬───────────────────────┐
│ id │ user_id │ name   │ phone        │ registered_at         │
├────┼─────────┼────────┼──────────────┼───────────────────────┤
│ 10 │ 5       │ Ahmad  │ 081234567890 │ 2026-01-07 15:00:00   │
└────┴─────────┴────────┴──────────────┴───────────────────────┘
      ↑
      └── Ter-link!
```

**Skenario B: Phone SUDAH ADA di customers (pernah transaksi)**
```
SEBELUM REGISTER:
TABEL customers:
┌────┬─────────┬────────┬──────────────┬───────────────┐
│ id │ user_id │ name   │ phone        │ registered_at │
├────┼─────────┼────────┼──────────────┼───────────────┤
│ 3  │ NULL    │ Ahmad  │ 081234567890 │ NULL          │
└────┴─────────┴────────┴──────────────┴───────────────┘
      ↑
      └── Belum ter-link

SESUDAH REGISTER:
┌────┬─────────┬────────┬──────────────┬───────────────────────┐
│ id │ user_id │ name   │ phone        │ registered_at         │
├────┼─────────┼────────┼──────────────┼───────────────────────┤
│ 3  │ 5       │ Ahmad  │ 081234567890 │ 2026-01-07 15:00:00   │
└────┴─────────┴────────┴──────────────┴───────────────────────┘
      ↑
      └── Sekarang ter-link ke user!
      
→ Ahmad bisa login dan lihat SEMUA transaksi lamanya!
```

---

## 🔄 ALUR 2: Pendaftaran via Google OAuth

### Langkah-langkah:

```
┌────────────────────────────────────────────────────────────────────┐
│  STEP 1: User klik "Login dengan Google"                          │
│          Google popup muncul, user pilih akun                      │
└────────────────────────┬───────────────────────────────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────────────┐
│  STEP 2: Google kirim JWT Token ke /api/google-auth.php            │
│          Token berisi: google_id, email, name, picture             │
└────────────────────────┬───────────────────────────────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────────────┐
│  STEP 3: CEK apakah user sudah ada di database                     │
│                                                                    │
│  SELECT * FROM users WHERE google_id = 'xxx' OR email = 'yyy'      │
└───────────────┬────────────────────────────┬───────────────────────┘
                │                            │
        ┌───────┴───────┐            ┌───────┴───────┐
        │  BELUM ADA    │            │  SUDAH ADA    │
        └───────┬───────┘            └───────┬───────┘
                │                            │
                ▼                            ▼
┌───────────────────────────┐    ┌───────────────────────────┐
│  INSERT user baru         │    │  UPDATE last_login        │
│  (tanpa phone!)           │    │                           │
│                           │    │  UPDATE users             │
│  INSERT INTO users        │    │  SET last_login = NOW()   │
│  (google_id, email, name, │    │  WHERE id = ?             │
│   profile_picture, role)  │    │                           │
│  VALUES (...)             │    │                           │
│                           │    │                           │
│  → phone = NULL           │    │                           │
└───────────────────────────┘    └───────────────────────────┘
                │                            │
                └──────────────┬─────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────────────┐
│  STEP 4: CEK apakah user punya PHONE                               │
│                                                                    │
│  if (empty($userData['phone']))                                    │
└───────────────┬────────────────────────────┬───────────────────────┘
                │                            │
        ┌───────┴───────┐            ┌───────┴───────┐
        │  BELUM PUNYA  │            │  SUDAH PUNYA  │
        └───────┬───────┘            └───────┬───────┘
                │                            │
                ▼                            ▼
┌───────────────────────────┐    ┌───────────────────────────┐
│  Redirect ke:             │    │  Redirect ke:             │
│  /pages/auth/             │    │  /pages/customer-         │
│    complete-profile.php   │    │    dashboard.php          │
│                           │    │                           │
│  User harus input HP      │    │  User langsung masuk! ✓   │
└───────────────────────────┘    └───────────────────────────┘
```

### Alur Complete Profile (untuk user Google tanpa HP):

```
┌────────────────────────────────────────────────────────────────────┐
│  /pages/auth/complete-profile.php                                  │
│                                                                    │
│  ┌──────────────────────────────────────────────┐                  │
│  │  Selamat Datang, Ahmad!                      │                  │
│  │                                              │                  │
│  │  Email: ahmad@gmail.com (readonly)           │                  │
│  │                                              │                  │
│  │  No. HP: [_______________]                   │                  │
│  │                                              │                  │
│  │  [ Simpan & Lanjutkan ]                      │                  │
│  └──────────────────────────────────────────────┘                  │
└────────────────────────┬───────────────────────────────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────────────┐
│  POST ke /api/auth/update-phone.php                                │
│                                                                    │
│  1. UPDATE users SET phone = '081234567890' WHERE id = 5           │
│                                                                    │
│  2. CEK: Phone ada di customers?                                   │
│     - ADA → UPDATE customers SET user_id = 5 WHERE phone = '...'   │
│     - TIDAK → INSERT customers (user_id, name, phone, ...)         │
│                                                                    │
│  3. Redirect ke dashboard                                          │
└────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 ALUR 3: Admin Tambah Customer

### Langkah-langkah:

```
┌────────────────────────────────────────────────────────────────────┐
│  Admin buka /pages/customers.php → Tambah Pelanggan                │
│  Isi: Nama, Phone, Alamat                                          │
└────────────────────────┬───────────────────────────────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────────────┐
│  POST ke /api/customers-api.php?action=create                      │
└────────────────────────┬───────────────────────────────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────────────┐
│  CEK: Apakah Phone sudah ada di USERS?                             │
│                                                                    │
│  SELECT id FROM users WHERE phone = '081234567890'                 │
└───────────────┬────────────────────────────┬───────────────────────┘
                │                            │
        ┌───────┴───────┐            ┌───────┴───────┐
        │  TIDAK ADA    │            │  SUDAH ADA    │
        └───────┬───────┘            └───────┬───────┘
                │                            │
                ▼                            ▼
┌───────────────────────────┐    ┌───────────────────────────┐
│  INSERT customer          │    │  INSERT customer dengan   │
│  TANPA user_id            │    │  user_id TER-LINK!        │
│                           │    │                           │
│  INSERT INTO customers    │    │  INSERT INTO customers    │
│  (name, phone, address)   │    │  (user_id, name, phone,   │
│  VALUES (...)             │    │   address, registered_at) │
│                           │    │  VALUES (5, ..., NOW())   │
│  → user_id = NULL         │    │                           │
│  → Status: "Belum Daftar" │    │  → Status: "Terdaftar" ✓  │
└───────────────────────────┘    └───────────────────────────┘
```

---

## 📊 Diagram Relasi Database

```
┌─────────────────────┐          ┌─────────────────────┐
│       USERS         │          │     CUSTOMERS       │
├─────────────────────┤          ├─────────────────────┤
│ id (PK)             │←─────────│ user_id (FK)        │
│ email               │          │ id (PK)             │
│ name                │          │ name                │
│ phone ──────────────┼──MATCH───│ phone               │
│ password_hash       │          │ address             │
│ google_id           │          │ registered_at       │
│ role                │          │ created_at          │
└─────────────────────┘          └──────────┬──────────┘
                                            │
                                            │
                                 ┌──────────┴──────────┐
                                 │    TRANSACTIONS     │
                                 ├─────────────────────┤
                                 │ id (PK)             │
                                 │ customer_id (FK)────┘
                                 │ service_type        │
                                 │ weight              │
                                 │ price               │
                                 │ status              │
                                 │ created_at          │
                                 └─────────────────────┘
```

---

## ✅ Manfaat Integrasi

| Fitur | Penjelasan |
|-------|------------|
| **Auto-link by Phone** | User register → otomatis terhubung ke customer jika phone sama |
| **Lihat Transaksi** | User login → bisa lihat semua transaksi berdasarkan phone |
| **Status Terdaftar** | Admin lihat status "Terdaftar"/"Belum Daftar" di halaman customers |
| **Invite Customer** | Admin bisa kirim link registrasi ke customer yang belum daftar |
| **Google + HP** | User Google diminta lengkapi HP untuk linking |

---

*Dokumentasi ini menjelaskan alur Customer-User Integration di D'four Laundry System*
